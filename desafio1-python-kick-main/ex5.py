# Faça um programa que calcule a área de um quadrado/retângulo.

base = float(input('Qual a medida da base: '))
altura = float(input('Qual a altura: '))

area = base * altura

print(f'A área do quadrado/retângulo é de {area}')